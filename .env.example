# Copie este arquivo para `.env` na raiz do projeto e preencha com os valores do ambiente.
# O `.env` real não deve ser versionado (está no .gitignore).

# Host do painel Kotlin (obrigatório). Login/catálogo usam rotas .php neste host.
EBOOK_SITE_BASE_URL=https://admin.alenxandriaglobaltec.com/

# Opcional — se omitido, usa {site}api.php
# EBOOK_API_BASE_URL=https://admin.alenxandriaglobaltec.com/api.php
